function [Ar]=ACA(A)
    global eps;
    k=0;
    R=A;
    U0 = [];
    V0 = [];
    while (frobenius(R)<=eps*frobenius(A))
        k=k+1;
        [val, i] = max(A);
        [val, j] = max(val);
        i = i(j);
        uk = 
    end
end 